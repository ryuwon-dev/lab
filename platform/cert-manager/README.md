# cert-manager

Certificate automation manifests live here.

Only public-safe issuer and certificate configuration should be committed.
Provider tokens, account keys, and private key material must stay out of Git.
