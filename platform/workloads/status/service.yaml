apiVersion: v1
kind: Service
metadata:
  name: status
  namespace: status
  annotations:
    argocd.argoproj.io/sync-wave: "10"
  labels:
    app.kubernetes.io/name: status
    app.kubernetes.io/instance: status
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: status
    app.kubernetes.io/instance: status
  ports:
    - name: http
      port: 80
      targetPort: http
