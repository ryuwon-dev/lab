apiVersion: v1
kind: Service
metadata:
  name: portal
  namespace: portal
  annotations:
    argocd.argoproj.io/sync-wave: "10"
  labels:
    app.kubernetes.io/name: portal
    app.kubernetes.io/instance: portal
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: portal
    app.kubernetes.io/instance: portal
  ports:
    - name: http
      port: 80
      targetPort: http
